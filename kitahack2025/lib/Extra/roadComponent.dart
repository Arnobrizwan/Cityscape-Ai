import 'package:flutter/material.dart';

class RoadComponent extends StatelessWidget {
  const RoadComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Road');
  }
}
