import 'package:flutter/material.dart';

class TrafficComponent extends StatelessWidget {
  const TrafficComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Traffic');
  }
}
